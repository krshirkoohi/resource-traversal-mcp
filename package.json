{
    "name": "resource-traversal-mcp",
    "version": "1.0.0",
    "description": "MCP server for traversing authenticated web resources using headless browser sessions",
    "type": "module",
    "main": "index.ts",
    "scripts": {
        "start": "bun run index.ts",
        "dev": "bun --watch index.ts",
        "auth": "bun run scripts/auth-setup.ts"
    },
    "dependencies": {
        "@modelcontextprotocol/sdk": "^1.0.0",
        "playwright": "^1.50.0"
    },
    "devDependencies": {
        "@types/node": "^22.0.0",
        "bun-types": "^1.3.9"
    }
}
